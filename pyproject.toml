[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "radiocodes"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = ["PySide6>=6.5.0"]
